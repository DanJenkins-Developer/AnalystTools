- Make queries editable (Typos)

- If PID is entered with a comma, remove it. 

- Add format: "Execution went `` -> `` -> `` -> `` -> ``"

- Get file directories

Get directory response action. Create powershell script that gets the size of a directory (or size of all files 
together within a directory). If the size is below a certain threshold, zip the contents of the directory and store
in a temporary location that can be downloaded from. 